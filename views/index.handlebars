<div class="container">
    <div class="row">
        <div class="col s12">
            <h3>Burger Lover !!</h3>
        </div>

        <div class="row">
            <div class="col s6 to-devour">
                <ul class="collection with-header">
                    <li class="collection-header">
                        <h5>Burgers to Eat</h5>
                    </li>
                    {{#each burgers}}
                    {{#unless devoured}}
                    {{> burgers/burger-block devoured=false}}
                    {{/unless}}
                    {{/each}}
                </ul>
            </div>

            <div class="col s6 devoured">
                <ul class="collection with-header">
                    <li class="collection-header">
                        <h5>Burgers Eaten</h5>
                    </li>
                    {{#each burgers}}
                    {{#if devoured}}
                    {{> burgers/burger-block devoured=true}}
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="row formdiv">
            <form class="col s8 form offset-s2">
                <div class="row formdiv">
                    <div class="input-field col s12">
                        <textarea id="textarea" class="materialize-textarea"></textarea>
                        <label for="textarea">Enter a burger you want to eat</label>
                    </div>
                </div>
            </form>
        </div>
        <div class="row submitdiv">
            <button class="btn waves-effect waves-light submit" type="submit" name="action">Submit
                <i class="material-icons right">send</i>
            </button>
        </div>

    </div>
</div>